---
streamId: "account-3434922/locations"
urlFormat: "{{ .externalEntityId }}.html"
---


{{define "product.source" -}}
<html>
    <body>
        <h1>{{.name}}</h1>
        <p>SKU: {{.sku}}</p>
        <img src={{.photoGallery}} alt="photo">
        <p>Description:{{.richTextDescription}}</p>
        <div>
            <span>{{.size}}</span>
            <span>{{.price}}</span>
        </div>
    </body>
</html>
{{end}}

