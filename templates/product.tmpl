---
{
    "streamId": "pages-product-stream",
    "urlFormat": "{{.c_slug}}"
}
---

{{define "product.tmpl" -}}
<html>
    <body>
        <h1>{{.name}}</h1>
        <h2>product info</h2>
        <p>SKU: {{.sku}}</p>
        <p>Description:{{.richTextDescription}}</p>
        <div>
            <span>{{.size}}</span>
            <span>{{.price}}</span>
        </div>
    </body>
</html>
{{end}}
